{
  "name": "vital-sync",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "mobile",
    "admin",
    "web"
  ],
  "overrides": {
    "graphql": "16.8.1"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "scripts": {
    "dev": "concurrently \"npm start --workspace=backend\" \"npm run dev --workspace=web\""
  }
}
